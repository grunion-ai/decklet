# The editor

[SKILL.md](../SKILL.md) is the authoring contract: content in, a verified model out. This file is the other half — what the deck file does once a person opens it in a browser. An agent building a model needs none of it until the hand-off, so quote the two or three lines that matter and link here for the rest.

## The HUD

**HUD (the full set, left to right), four groups after the spacer, a hairline between groups; every control names itself and its key on hover (`data-tip`, instant — never the OS title delay):** prev / next (chevrons; ← →) · **save:** the save button (its own button, a Lucide save glyph the size of every other control: calm = the file has everything · amber with a count badge = saved in this browser, N edits not in the file yet · red with a **!** = nothing persists · a pulse while a write is in flight; a click or tap saves the file now — the ⌘S door on a phone, and the only Save-a-copy door: where the browser blocks storage, ⌘S and this button both download a copy, and the tooltip says so) · **edit:** `+` (Text / Box / Slide) · duplicate (copy-plus icon: the selected rows, offset 16px, the copies become the selection · else the slide, after itself · in the sheet, the selected slides) · grid + guides + snap (grip icon; **G**; off by default, dims when off: a 16px dot grid over the slide and this slide's layout lines — slot edges, the content margin, the canvas mid-lines — of which only the line being held shows; a drag, a resize or a connector nib snaps to a line within 10px, else to the grid; a circle snaps by its centre; a connector end also snaps to the eight 45° rays out of its other end; never in present, print, the sheet, the PDF or the saved file) · spellcheck (spell-check icon; on by default, dims when off: the words the build's dictionary refused — nspell + dictionary-en at create, optional — are marked on every slide through the CSS Highlight API with a wavy underline **and** a faint red wash, so they show in Chrome, in Safari, which paints no text-decoration inside a highlight pseudo, and in an embedded pane alike; the browser's own checker adds the row you type in; never present, print or PDF) · the red badge on that button is a button of its own: it opens a panel of every flagged word on this slide, each with the suggestions the build baked into the file — a pick replaces every occurrence of that word on the slide, keeping its case and its markup, through the ordinary edit path (so it lands in the file and in `bin/edits.mjs`), and **Ignore in this deck** appends the word to the model's `spell.ignore` and repaints. A click on a marked word while you are editing its row opens the same panel anchored to it and fixes that one occurrence; Escape closes it and hands focus back. The badge counts this slide live as you type, on the contact sheet every thumbnail wears its own count at the top-right, and zero shows nothing · **file:** PDF (⤓) · **view:** contact sheet (grid icon; **C** or Esc) · full screen (**F**) · shortcuts (ⓘ; a three-column popover: slides · rows · file; ⌘P and ⌘D are not shortcuts — the PDF and duplicate buttons are the only doors) · **Report a bug** (bug icon; no key — a report is never one keystroke away; hidden from the present-mode peek pill so it clears the page counter; opens a native dialog: four tiles — Looks wrong · Won't respond · Save or PDF · Crashed — and a 300-character description; **Open mail app** is an anchor whose `mailto:decklet@grunion.ai` is rebuilt on every change, **Copy report** puts the same text on the clipboard; subject `[ISSUE] decklet · <category> · <first 70 chars of the description>`; body: the engine version, format, slide size and count, current slide, mode, the browser UA, the deck id, viewport · dpr · colour scheme, then the category's probes — counts and fixed words only: rows by kind and clipped rows for Looks wrong, pointer · selection · autosave for Won't respond, autosave · file access · versions · pdf route for Save or PDF, the last three uncaught errors (scrubbed) · uptime · drawn rows for Crashed — then the description; never a word, note, alt, token, image, link, path or edit of the deck; the reporter reads and sends it, the address is receive-only).
<!-- HUD: prev next autosave savebad addbtn dup snap spell spellbad pdf grid-btn fs help bug -->
This manifest is a contract: the gate compares it against the template, so the HUD cannot gain or lose a control without this line changing. While presenting, the HUD peeks back as a centred pill above the bottom edge — never over the page counter in the corner. On the contact sheet the HUD stays, pinned above the thumbnails, with prev/next/present disabled (the sheet is the navigator) and `+` adding a slide after the current one. Drag to move (a connector travels whole — both ends and every control point; a `group` travels whole and wears one dashed box), ⌘-click to multi-select (or to take one member of a group alone), drag from empty canvas for a marquee that takes every row it wholly contains (⇧ adds to the selection), double-click to retype, corner nib to resize, a connector's **point nibs** move one end (or a curve's control point) while a drag on its shaft moves it whole, ⌘Z to undo (persists across reloads), ⌘B / ⌘I / ⌘U mark a text selection or a whole selected row. Selecting text shows a floating toolbar: role segment, **B / I / U / S̶ / link** (marks never change size; the link takes http, https or mailto — an empty field unlinks), the deck's own colours as swatches, "Apply to all slides".

## On a phone

**On a phone:** the shell fits the layout viewport (never the inflated `innerWidth`), respects the safe area, and sizes with `svh`; test/touch.test.mjs is the phone lane (iPhone-sized, touch, coarse pointer). Under `(pointer: coarse)` the resize and connector nibs are 28px on screen whatever the slide's scale (`fit()` publishes the scale as `--S`; the nibs divide by it) and HUD and toolbar buttons are 44px tall; a fine pointer keeps the 13px nibs. Presenting, a finger turns the page: a horizontal swipe goes next or previous, a tap advances (the left fifth goes back), a tap in the bottom twelfth peeks the HUD, and a tap on a link follows it; editing has no page gesture.

## The contact sheet

**Contact sheet:** live thumbnails 3-across; click / ⌘ / shift select, double-click opens, grab-and-drag reorders (mouse or touch — the other cells slide aside), ⌫ deletes (never the last), ⌘C ⌘V ⌘D ⌘Z. It also opens in present mode.

## PDF

**PDF:** links become real `/Link` annotations, so a LinkedIn document post is clickable. **Agents make PDFs one way: `node bin/pdf.mjs deck.html [out.pdf]`** — the deck's own print pipeline through Chromium's print engine with a pixel `@page`, so every page is the slide's size (1 px = 0.75 pt), text stays text, fonts embed, and the HUD is hidden by the deck's `@media print` (never by an ad-hoc override). It gates itself (exit 1): pages == slides, page ratio == W/H, `#hud` computed `display:none` under print media, link annotations == linked rows. Screenshots stitched into a PDF (Playwright + ImageMagick) are an anti-pattern: raster text blurs on every rescale, carries no links, and shipped the HUD once. **The PDF button** takes the same route in Chromium — `@page{size:W H px}` + `zoom:1` injected, then the browser's print dialog, where the user picks "Save as PDF"; `afterprint` removes the rule. **Safari** ignores px `@page` sizes, so there the button keeps the in-file rasteriser: each slide from its live DOM (SVG `foreignObject` → canvas at 3× (2× above 40 slides) → lossless RGB `FlateDecode` via `CompressionStream`; a page whose deflate exceeds 6 MB drops to JPEG .92) onto a W×H pt page — no letterboxing, no JPEG ringing; verified in WebKit. While it runs the ⤓ button reads `n / N` over a hairline bar and the loop yields after every page, so the count paints. If rasterising throws or the canvas is tainted, the button comes back and it falls back to `print()`. **⌘P is the paper path:** one page per slide on a **named** page size — Letter (A4 for `document-a4`) — the slide zoomed to the printable width.

## PNG export

**PNG export:** `node bin/export.mjs deck.html --png [--out dir] [--scale 2]` = one PNG per slide at the slide's own size (`--scale 2` for retina), named `01-<slide name or slide-1>.png`, no HUD, no selection chrome, no spell marks, animations settled, the page counter kept. **Agents use this for per-card carousel posts, never screenshot stitching** — a hand-shot card ships the HUD, a selection box or a half-played entrance. It gates itself (exit 1): `#hud` computed `display:none` at shoot time, files == slides, every PNG is W×H×scale. Default out: `<deck>-png/` beside the deck. It is `verify`'s screenshot loop on its own (verify hides the counter for AE parity; export keeps it).

## Presenting

**Presenting:** F or the fullscreen button; chrome hides, backdrop = current slide's background, HUD peeks back when the pointer rests at the bottom edge and stays pinned while the + menu, the text toolbar or the contact sheet is open. Arrow keys / space advance; Esc opens the contact sheet to jump.

## Persistence, honestly

**Persistence, honestly.** Every edit autosaves to the browser's storage under `deck.id` — localStorage; IndexedDB where localStorage is refused (a cross-site iframe); memory as the last resort — and is appended to the in-file **edit log** (slide id, row id, keys before → after). This holds on a desktop, on a phone (a touch edit persists; switching apps commits the edit in flight) and inside an iframe (same origin, srcdoc, or cross-site on whichever tier the browser allows). Every window or tab of the same browser showing the deck picks up an edit live (`storage` events + a BroadcastChannel): last write wins, each tab keeps its own slide, an edit in flight lands on top of what arrived, and a window that hears an edit never writes back. **The deck file writes itself:** in Chrome/Edge the first ⌘S (or a tap on the dot) asks for the file once (File System Access, remembered per browser), and from then on every edit rewrites it in place on an 800 ms trailing debounce — ⌘S, the dot, a hidden tab or an unload flush the pending write now — with the model and the log inside, so an agent reading the file sees exactly what the human did. The dot is green only when the file has everything; amber counts the edits that have not reached it. Safari (and any browser without File System Access) cannot write the file: there ⌘S downloads a self-contained copy, and on `file://` Safari refuses storage entirely — the dot goes red at load and the Save-a-copy button appears. A live text edit or a drag in flight is committed when the tab hides or unloads, so a refresh mid-edit loses nothing.
**Position:** a new window opens on slide 1; a refresh, and a new version of the file, keep the slide you were on — by slide id, so inserting slides above does not move you. A stored working copy is trusted only on the same `rev`; a newer file wins as the base and the browser replays its own edit log onto it (human wins, conflicts flagged on the entries), so an agent's rewrite neither hides its changes nor drops the human's.
**Versions:** none in the file since 0.9.0. Version history is weave's job; decklet keeps the edit log, which `create --from` replays.

## Revising an edited deck

**Revising a deck a human has touched — `--from` is mandatory.** The deck file carries the human's edits (`/*LOG*/`). Run, in this order:
```
node bin/edits.mjs deck.html                       # 1. READ what the human changed — before you touch the model
node bin/create.mjs --model model.json --out deck.html --from deck.html   # 2. write the new version LAST
node bin/verify.mjs deck.html                      # 3. verify the result
```
`--from` inherits the deck id and the slide/row ids (by content, then by position — carry ids in `model.json` to make it exact), replays every logged edit onto the new model (**human wins**; a key you also changed is reported as a conflict and the human's value stays — put yours in the log's `conflict` if it matters), so nothing the human did can be lost by a rebuild. The diff-and-migrate is the last thing you do, never the first: read the log, keep their geometry and text, then regenerate. A deck built before 0.5.0 has no log: `--from` still carries its id, replays nothing, and says so. A deck built by 0.5–0.8 carries a `/*VERSIONS*/` history block; `--from` reads past it — the history left the file in 0.9.0 (it is weave's job).

Everything a human applies in the editor — geometry, text, links, arrows — round-trips that way; the console `copy(JSON.stringify(deck))` still works for a raw model.

## Motion

Four words, and no fifth: `rise` (text — the default), `fade` (quiet chrome), `pop` (stats, tiles, images), `wipe` (bars, lines, rules). Anything else is an error in `validate` and is ignored by the renderer.

- **Entry only.** Animated rows enter in model order, staggered 120 ms, and only when a slide is *entered*. Dragging, selecting, retyping or undoing re-render the same slide without restaggering it. Order the rows the way you want them to arrive.
- **Never in the artifact.** Print, the contact sheet, the `⤓` PDF and `verify` all draw the settled frame — motion cannot change what is measured, printed or exported.
- **Off is a first-class state.** `prefers-reduced-motion: reduce` turns every anim off; a deck must read exactly the same standing still.
- **Motion is punctuation, not decoration.** Animate the rows that carry the beat of the slide (title, then the two or three rows the audience should read in order). A slide where every row moves reads as noise — and a wall of `anim` is a review failure.
- Pair a row with its label (tile then caption, both with the same anim) so they arrive together instead of the labels landing four steps later.

## Reporting a bug

The bug door is the HUD's last control, above. `node bin/bug.mjs --category <kind> --desc "…"` prints the same prefilled `mailto:decklet@grunion.ai` from the command line. The address is receive-only, and a report never carries a word, note, alt text, token, image, link, path or edit of the deck.
